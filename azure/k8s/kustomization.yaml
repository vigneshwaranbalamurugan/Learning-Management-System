apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Apply all manifests in order with:
#   kubectl apply -k azure/k8s/
resources:
  - namespace.yaml
  - configmap.yaml
  - serviceaccount.yaml
  - secretproviderclass.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml


